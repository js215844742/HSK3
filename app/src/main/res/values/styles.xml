<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyDialogStyle" parent="@style/Theme.AppCompat.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item><!--窗口背景色透明-->
        <item name="android:windowFrame">@null</item><!--没有windowFrame框-->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item><!--悬浮-->
        <item name="android:windowIsTranslucent">true</item><!--窗口半透明-->
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item><!--窗口弹出效果-->
        <item name="android:backgroundDimEnabled">true</item><!--背景模糊-->
    </style>

    <style name="animationHead">
        <!-- PopupWindow左右弹出的效果 -->
        <item name="android:windowEnterAnimation">@anim/down</item>
        <item name="android:windowExitAnimation">@anim/up</item>
    </style>
</resources>
